[package]
name = "dlog_core"
version = "1.0.2"
authors = ["Lennart Voorgang <lennart@voorgang.dev>"]
edition = "2018"
license = "MIT"
description = "Core library for the dlog logging platform"
homepage = "https://app.dlog.cloud"
repository = "https://github.com/lennartvrg/dlog"
readme = "README.md"

[dependencies]
regex = "1.5.4"
futures = "0.3.14"
static_init = "1.0.1"
tokio = { version = "1.5.0", features = ["time"] }
serde_json = { version = "1.0.64", default-features = false }
flume = { version = "0.10.5", default-features = false, features = ["async"] }
chrono = { version = "0.4.19", default-features = false, features = ["clock", "serde"] }
serde = { version = "1.0.125", default-features = false, features = ["derive"] }
reqwest = { version = "0.11.3", default-features = false, features = ["json", "blocking", "rustls-tls"] }
